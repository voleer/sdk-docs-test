# Voleer 'login' command

The `login` command signs into your Voleer account.

```shell
>voleer login --help
log in to your Voleer account

USAGE
  $ voleer login

OPTIONS
  --no-browser     whether to not open browser for logging in
  --tenant=tenant  (required) tenant name
```
